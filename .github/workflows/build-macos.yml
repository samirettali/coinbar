name: Build macOS App

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode.app/Contents/Developer

      - name: Build unsigned app
        run: ./scripts/build-release.sh

      - name: Upload Coinbar app bundle
        uses: actions/upload-artifact@v4
        with:
          name: Coinbar.app
          path: build/export/Coinbar.app

      - name: Upload Zonebar app bundle
        uses: actions/upload-artifact@v4
        with:
          name: Zonebar.app
          path: build/export/Zonebar.app

      - name: Upload Coinbar zipped release artifact
        uses: actions/upload-artifact@v4
        with:
          name: Coinbar-macOS-unsigned
          path: build/Coinbar-macOS-unsigned.zip

      - name: Upload Zonebar zipped release artifact
        uses: actions/upload-artifact@v4
        with:
          name: Zonebar-macOS-unsigned
          path: build/Zonebar-macOS-unsigned.zip
